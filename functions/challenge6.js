// Create a function that takes two numbers as a parameters and takes the second one away from the first one.

const minus = (n1, n2, n3) => {
    return (n1 - n2 - n3)
}
console.log(minus(98, 23, 45));